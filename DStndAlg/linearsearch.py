


def linear_search(list, target):
    for i in range(0, len(list))
        if list[i] == target
            return i
             return none;

        
verify(index)
        if index is not none
            print("the index number is index: " index)
        else if index is none
            print("the index number is nt available");

numbers = [1, 2, 3, 4, 5, 7, 8, 9, 10]

result = linear_search(numbers, 12)

verify(result)


